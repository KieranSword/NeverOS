#include "kernel.h"
#include "colour.h"
#include "window.h"

int booted = 0;

int main(int argc, char argv[256]) {
	booted = 1;
	if(booted = 1) {
		boot();	
	}
	create_window(0,0,10,10,"Hi!\n");
	return 0;
}
